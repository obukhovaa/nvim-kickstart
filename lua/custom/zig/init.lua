return {
    {
        'ziglang/zig.vim',
        config = function()
            -- require('ziglang/zig.vim').setup {
            --     -- Configuration here, or leave empty to use defaults
            -- }
        end,
    },
}
